doctype html
html(lang="pt-br")
    head
        title Apagar usuário
        meta(charset="utf-8")
        link(rel="stylesheet", href="/static/hcw.min.css")
        script(src="/static/formulario.js")
        script(src="https://kit.fontawesome.com/089953a984.js", crossorigin="anonymous")
    body
        header.padding-16.raisin-black
            h1.text-center Apagar usuário
        
        include menu.pug

        section.margin-10.light-gray
            div.card
                h3.padding-16.raisin-black.text-center Formulário para apagar  usuário
                div.padding-16
                    form(method="post", action="/usuario/", onsubmit="return bloquear()")
                        label(for="id") Selecione qual deseja apagar:
                        select(id="id", name="id", class="input white border-bottom-blue-focus")
                            each item in usuario
                                option(value=item.id) #{item.nome} 

                        br
                        input(type="submit", class="btn btn-block red", onclick="enviar('DELETE', 'usuario')", value="Apagar")

        include ../footer.pug
    